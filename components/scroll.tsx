import styles from './scroll-styles.module.css'

const Scroll = () => {
    return (
        <button className={styles.scroll}>
            <span>

            </span>
        </button>
    )
}

export default Scroll